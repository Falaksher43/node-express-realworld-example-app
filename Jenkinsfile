  node {
    
    tools {nodejs "nodejs"}
    
        stage('Build') {
                sh 'npm install'
        }
        stage('Test') {
                sh 'npm test'
                }
        stage('Deploy') {
                echo "deploy"
                        }
    }
