node {
        stage('Build') {
                sh 'npm install'
        }
        stage('Test') {
                sh 'npm test'
                }
        stage('Deploy') {
                echo "deploy"
                        }
    }
