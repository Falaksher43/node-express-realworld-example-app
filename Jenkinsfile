node {
        stage('Build') {
                sh 'npm install --save-dev'
        }
        stage('Test') {
                sh 'npm test'
                }
        stage('Deploy') {
                echo "deploy"
                        }
    }
